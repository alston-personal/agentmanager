# Mio Threads operational runbook (2026-09-20, Asia/Taipei)

**Purpose:** Preserve the *working retrieval and execution path* across new chats, agents, and computers. Consult this file **before** saying that Oracle or GitHub Actions is inaccessible. This document records verified past observations; re-check current state at each handoff. Image publishing has been verified by actual Threads IMAGE readback; autonomous reply delivery and external-post discovery require separate evidence.

## Identifiers and boundaries

- Persona: 澪 / Mio; latest verified public post permalink is under `@mio.milkcat`. The legacy stored credential binding / some old receipts still say `@sunlake.milkcat`; check provider account ID and current identity before publishing, and never assume these strings are interchangeable without live verification.
- Code: `alston-personal/agentmanager`, live branch `core/integration`.
- Persona memory: `alston-personal/my-agent-data`, `main`, `personas/sunlake-milkcat/events/events.jsonl`.
- Shared runtime owns API, authorization, media, publishing and receipts. Persona owns content, policies, schedule and event references. Do not put credentials in persona repos or receipts.
- Image work: PR [#405](https://github.com/alston-personal/agentmanager/pull/405), feature branch `feature/mio-image-publish-20260920`; **do not merge or claim complete without testing real image transport, image readback and public URL**. Do not downgrade an IMAGE request to TEXT.

## Critically important: How to fetch *push* workflow results

`mcp__GitHub__fetch_commit_workflow_runs` **only returns PR-triggered workflow runs** in this connection. An empty result for a push commit does **not** mean Actions never ran. This exact mistake led to many false "not verified" responses.

1. Via connected GitHub `fetch`, read `https://api.github.com/repos/alston-personal/agentmanager/actions/runs` (a bare collection URL worked on 2026-09-20). Parse `workflow_runs[]` and filter by **`head_sha`, workflow `name`/`path`, and `event: push`**; follow pagination only as supported. This read endpoint is different from the refused `/actions/workflows/<workflow>.yml/runs?event=push&per_page=10` URL.
2. Use connected GitHub `fetch_workflow_run_jobs({repo_full_name:"alston-personal/agentmanager",run_id})`. Identify the actual publish/install job ID.
3. Use `fetch_workflow_job_logs({repo_full_name:"alston-personal/agentmanager",job_id})` and inspect **runtime output lines**, not merely the job status or echoed shell source. Require `mio_approved_receipt=PASS`, `galaxy_day1_publish=PASS` (or authenticated already-present verification), actual `galaxy_day1_object_id` and `galaxy_day1_permalink`; cross-check the Threads post and expected text. For image, require media_type IMAGE and actual visible image; for reply, require a real reply ID. Do not expose tokens or secrets.
4. Read the existing `.github/workflows/oracle-publish-mio-approved.yml` and `scripts/publish_galaxy_threads_day1_user.sh` from current `core/integration` before changing a workflow. Never blindly retry a post if its result is unknown; read the platform state by exact intent/object ID first.

## Confirmed successful push-run proof (2026-09-20)

- New original TEXT article `personas/mio/approved/mio-post-20260920-small-discoveries-v1.txt`, source commit `ea2adc001eabefa107f68ae33f8dc06778942c8c`.
- [Actions run 35484386298](https://github.com/alston-personal/agentmanager/actions/runs/35484386298), publish job `106007938741`; job log emitted `mio_approved_receipt=PASS`, `galaxy_day1_publish=PASS`, Threads ID `18042529955827294`, permalink `https://www.threads.com/@sunlake.milkcat/post/DdfhumnFHdz`.
- This establishes that the GitHub push → self-hosted Oracle runner → existing governed publisher → Threads *TEXT* path worked at that time. It does not establish image publishing or unattended recurring posts.

## Confirmed reply monitoring + memory, remaining blockers

- [Actions run 35486126362](https://github.com/alston-personal/agentmanager/actions/runs/35486126362), monitor install job `106012720999`: `galaxy_experiment_monitor_receipt=PASS`, monitor read 15 replies. It saw reader cat comment ID `18234417796318016` and *human-assisted* Mio reply ID `18368916244215087`, permalink `https://www.threads.com/@sunlake.milkcat/post/Ddfl8WvE2C0`.
- These two events were written to `my-agent-data` `main` in commit `19836b0dce4179cbc481e3e57e32e56664ea578b`. A manual reply can be a Persona event once observed; do **not** invent source or claim autonomous posting.
- The same monitor logs showed `mio_social_decision=DEFERRED:RuntimeError` and `mio_social_outbound=DISCOVERY_UNAVAILABLE:threads_api_unavailable_http_500...`. **Autonomous reply delivery has not been verified**; diagnose decision relay and search API separately. A monitor `PASS` or `pending=0` is not a successful reply receipt.
- Existing code: `scripts/monitor_galaxy_threads_experiment_user.py`, `scripts/mio_persona_social_loop_user.py`, `scripts/sync_sunlake_milkcat_persona_user.py`, `scripts/install_galaxy_threads_experiment_monitor_user.sh`. Monitor uses an experiment snapshot; make sure new comments are not lost on failed reads, decision failures, or snapshot replacement.

## Continuation contract

First retrieve this runbook, current branch/PR state, latest push workflow runs **via the collection**, then job logs. Report verified platform evidence, not the number of commits or a rephrased plan. If the specific capability remains blocked, give the exact failing step and relevant job/error. User should not have to repeat the above path.


## Verified new post and governed reply (2026-09-20)

- New approved TEXT post `mio-post-20260920-cat-hello-v1.txt`: [Actions run 35486982735](https://github.com/alston-personal/agentmanager/actions/runs/35486982735), `mio_approved_receipt=PASS`, `galaxy_day1_publish=PASS`, platform ID `17994092795835001`, permalink `https://www.threads.com/@sunlake.milkcat/post/DdfomfNFPih`.
- Approved reply to reader `vivian780927`'s comment ID `18100785275370622` ("看不懂"): [Actions run 35487296837](https://github.com/alston-personal/agentmanager/actions/runs/35487296837), `persona_threads_reply_receipt=PASS`, `persona_threads_reply_publish=PASS:18100785275370622:17978287527132218`, and readback-confirmed permalink `https://www.threads.com/@sunlake.milkcat/post/DdfphJrFCpy`. This was an **approved governed reply**, not an autonomous generated decision.
- Existing approved-reply path: create exactly ONE new `personas/mio/approved/replies/mio-reply-*.json` per push in `core/integration`; existing workflow `.github/workflows/oracle-publish-sunlake-persona-replies.yml` triggers. Manifest has `root_post_id`, `reply_to_id`, `comment_author`, `expected_comment_text`, `text`; runtime verifies target/author/exact text, checks for prior replies, uses stable intent and requires platform readback. Keep checkout `fetch-depth: 0` and inline Python preflight compile; otherwise historical failures [35487025360](https://github.com/alston-personal/agentmanager/actions/runs/35487025360) and [35487064759](https://github.com/alston-personal/agentmanager/actions/runs/35487064759) recur. Failed reply create/publish [35487130528](https://github.com/alston-personal/agentmanager/actions/runs/35487130528) returned code 24.
- Core shared Threads provider repair: [social runtime rollout 35487237651](https://github.com/alston-personal/agentmanager/actions/runs/35487237651) succeeded, with same-container retry on publish code 24, never recreating a container. Subsequent approved reply succeeded as above.
- Verified records were appended to `alston-personal/my-agent-data` `main` `personas/sunlake-milkcat/events/events.jsonl`, commit `bf78ad0015a720fcfdc99e22df20884b3f4e5fdc`.
- **Still open:** automatic reply decisions use the Antigravity relay and are failing with `persona_decision_executor_failed:provider=claude:returncode=none:timed_out=false:category=other` in [monitor run 35487397344](https://github.com/alston-personal/agentmanager/actions/runs/35487397344). An approved reply delivered via the governed path does NOT establish fully autonomous replies. Durable history replay is installed; it should preserve unprocessed comments, but must be tested with a new real comment + automatic decision and receipt.

## Autonomous reply relay recovery (2026-09-20)

- Root cause observed, not guessed: [monitor run 35487730369](https://github.com/alston-personal/agentmanager/actions/runs/35487730369) showed `persona_decision_executor_failed:provider=claude:returncode=none:timed_out=false:category=other:error_type=FileNotFoundError:errno=2:missing_file=claude:exists=false`. The ubuntu-owned relay had selected a vanished Claude executable. A fresh discovery on Oracle found both Claude and AGY candidates available ([run 35487882486](https://github.com/alston-personal/agentmanager/actions/runs/35487882486)).
- A targeted existing ubuntu-owned `agentos-antigravity-relay.service` restart was attempted by source commit `718723a3301341adf962068debb018c710ce7b2d`; the combined installer timed out because the old decision call synchronously waited up to 150 seconds. **Do not treat that timed-out install as proof of restart success.** Subsequent live monitor showed a new decision capsule in `processing` and then `receipt_ready` ([run 35488361314](https://github.com/alston-personal/agentmanager/actions/runs/35488361314)); this is relay progress, not automatically a successful decision.
- The Persona decision is now **nonblocking and durable**: `scripts/mio_persona_social_loop_user.py` persists `decision-pending.json`, reuses the same capsule across 10-minute passes, never blindly resubmits on each monitor cycle, and discards a pending *decision-only* capsule if the original comment is no longer eligible or has already received a human-assisted reply. A monitoring install now finishes without waiting for model completion ([run 35488117239](https://github.com/alston-personal/agentmanager/actions/runs/35488117239)).
- Historical comments were inadvertently treated as new because the platform emits timestamps with `+0000`, while the original parser fell back to `now` on parse error. The explicit `datetime.strptime(...,'%Y-%m-%dT%H:%M:%S%z')` fallback and age filter prevent replies to stale comments; do not remove this safety fence. No fresh, unanswered eligible comment was present on the last monitored pass ([run 35488427170](https://github.com/alston-personal/agentmanager/actions/runs/35488427170)).
- One private no-side-effect CLI model health probe is in `scripts/mio_persona_social_loop_user.py` using `relay-probe.json`; it never posts to Threads. Inspect `mio_social_decision=RELAY_PROBE_PASS/FAILED` in the latest monitor Actions job log. A queued/processing probe is not a passing health check. **Do not claim fully autonomous reply success without a fresh externally authored comment, a model-authored decision, an actual Threads reply ID and readback.**

## Mio scoped AGY relay (2026-09-20)

- An isolated, side-effect-free AGY CLI smoke test ran under ubuntu and reported `mio_agy_probe=pass` in [Actions run 35490383633](https://github.com/alston-personal/agentmanager/actions/runs/35490383633). It checks a model-generated JSON fragment only and is not itself proof of autonomous Threads replies.
- Mio's **own** systemd user relay `agentos-mio-agy-relay.service` was installed as ubuntu after that probe, pointing at `/home/ubuntu/agent-data/runtime/mio-antigravity-relay` and `--provider agy`. [Install run 35490531834](https://github.com/alston-personal/agentmanager/actions/runs/35490531834) logged `mio_agy_relay=ACTIVE`, `galaxy_experiment_monitor_receipt=PASS`. This does **not** change global `agentos-antigravity-relay.service` or other AgentOS projects.
- The Mio script selects this isolated relay through `AGENTOS_MIO_RELAY_ROOT`, retaining separate `agy-decision-pending.json` and `agy-relay-probe-v2.json`. Existing shared Claude decision capsules are not replayed.
- First AGY health probe returned `returncode=0` but `invalid_result`; the CLI's structured output needs parsing independently from process success. Code commits `ba7470802b0a59c1fa3cbe45de63862c86912267` and `441bfe523689d5d4c1f0d61e2cd7df78384ddd50` add envelope-aware JSON extraction and a computed private health challenge. [Follow-up Actions run 35490640366](https://github.com/alston-personal/agentmanager/actions/runs/35490640366) was queued when written: check actual log before claiming health probe PASS.
- End-to-end autonomous reply acceptance still requires an actual new eligible reader comment → model decision → scheduled reply → real Threads reply ID + readback. The earlier [approved reply 35487296837](https://github.com/alston-personal/agentmanager/actions/runs/35487296837) is verified but is NOT an autonomous model-authored reply.

## New reader interaction and approval path (2026-09-20)

- Reader comment `18124006117843631` from `c.lara.ch` appeared on *owned* root post `18111065294165212`, discovered via [monitor run 35491689878](https://github.com/alston-personal/agentmanager/actions/runs/35491689878) and root verified in [run 35491830845](https://github.com/alston-personal/agentmanager/actions/runs/35491830845). Do not guess the parent post from previously published post IDs.
- The guarded approved reply manifest `personas/mio/approved/replies/mio-reply-20260920-c-lara-nap-v1.json` was sent through [Actions run 35491983539](https://github.com/alston-personal/agentmanager/actions/runs/35491983539), which logged `persona_threads_reply_receipt=PASS`, `persona_threads_reply_publish=PASS:18124006117843631:18437642710180998`, readback permalink `https://www.threads.com/@sunlake.milkcat/post/Ddf2HtHlL63`. **This is a governed approved reply, not an autonomous reply.**
- `scripts/publish_sunlake_persona_replies_user.sh` no longer hardcodes the first test post and commenter: for new reviewed replies, it verifies `root_post_id` is among the bound account's own posts and verifies the exact target comment ID, author, and text before accepting the write. It checks for any prior reply to that comment and requires actual platform readback. Source commit `894a21e009a3931fc77eef0981deaf01716a227d`.
- New verified reply event `threads-18437642710180998` stored in `my-agent-data` `main` at [commit 752a7b68069daf04d100ba71bfd390e5a37974df](https://github.com/alston-personal/my-agent-data/commit/752a7b68069daf04d100ba71bfd390e5a37974df); its context summary avoids needlessly reproducing the reader's health details.
- Auto-decision still failed to return a valid decision for the actual comment even though isolated AGY health probe passed; [run 35491917487](https://github.com/alston-personal/agentmanager/actions/runs/35491917487) saw `persona_decision_missing_expected_ids`. Source commit `33c7e7f2cd3a66691449102efd024dadae3e4d1f` guards against echoed model prompt templates, rejects decisions lacking an actual pending comment ID, and checks generated reply length. [Monitor run 35492156485](https://github.com/alston-personal/agentmanager/actions/runs/35492156485) passed installation and ended with `SKIP_ALREADY_REPLIED:18124006117843631`, protecting against duplicate delivery. **Still no evidence of a fully autonomous model-created and published reply.**

## Stabilization checkpoint: governed publishing and monitored replies

As of 2026-09-20 (Taipei), distinguish *each* capability before reporting that Mio is fully autonomous:

| Capability | Evidence | Conclusion |
|---|---|---|
| Approved text post, trigger = **new** `mio-post-*.txt` on `core/integration` | [run 35486982735](https://github.com/alston-personal/agentmanager/actions/runs/35486982735), Threads object `17994092795835001` | Verified platform post, **not** automatic recurring content creation |
| Approved reply, trigger = **new** `mio-reply-*.json` on `core/integration` | [run 35491983539](https://github.com/alston-personal/agentmanager/actions/runs/35491983539), reply object `18437642710180998`, readback | Verified platform reply, **not** model-authored autonomous reply |
| Regular reply monitoring | ubuntu systemd `agentos-galaxy-experiment-monitor.timer` configured `OnUnitActiveSec=10min`; [run 35492845802](https://github.com/alston-personal/agentmanager/actions/runs/35492845802) monitor receipt PASS | Deployed and verified during installer run; ongoing timer health requires fresh logs |
| Private AGY model executor | [run 35490640366](https://github.com/alston-personal/agentmanager/actions/runs/35490640366) `RELAY_PROBE_PASS`; isolated AGY daemon reported active in subsequent monitors | Health probe works, **not** proof of autonomous reply |
| End-to-end autonomously decided and published reply | No model-decision → scheduled item → Threads ID + exact readback receipt available | **NOT ACCEPTED**. Do not label complete or conflate with reviewed reply |
| Autonomous recurring original posts, image posts | No separately verified recurring writer + actual public post receipt, or actual visible image | **NOT ACCEPTED** |

Regression hardening performed: [run 35492732720](https://github.com/alston-personal/agentmanager/actions/runs/35492732720) passed six AGY decision / echoed schema / manual reply-dedup unit tests and monitor deployment. [run 35492845802](https://github.com/alston-personal/agentmanager/actions/runs/35492845802) passed all **eight** regression tests and `galaxy_experiment_monitor_receipt=PASS` after additional autonomous reply readback checks. Preflight now blocks deploy if these regressions fail. These are **unit / deployment checks**, not simulated Threads success.

Autonomous reply delivery safety: source commit `29b07dbfa6e3ffdbd91bf7e56cd8b145612ed022` requires matching platform reply ID, parent and text before a generated reply is marked sent; uncertain API responses enter `verification_pending` and are **not blindly retried**. A subsequent observed platform readback can finalize such an item. When ambiguous, favor pending review instead of duplicate public posts.

**Continuation criteria:** Never report "發文留言全通、完全固化" from a green monitor workflow alone. Reverify current post and reply receipts independently; when a fresh user comment arrives, demand a logged model decision, a scheduled action, real publication object ID, exact parent/text readback, and eventual Persona memory ingestion. The timer runs without opening a chat, but the approved-content push triggers still need an author/approval step.


## First live IMAGE + text post (2026-09-20)

- **Verified real Threads publication:** `https://www.threads.com/@sunlake.milkcat/post/DdgAUKMlH-X`, platform post ID `18356065492302036`. This was an illustrated landscape + caption via the *approved/governed* workflow, not autonomous image creation or automatic daily posting.
- IMAGE feature implemented in merged [PR #406](https://github.com/alston-personal/agentmanager/pull/406), merge commit `94701ebc5dd864c790f943b5ff9bd2b8febbc87a`. It adds validated `image_url` and `image_alt_text` to provider-neutral SocialRequest, IMAGE media container readiness and same-container publish, and media_type IMAGE + HTTPS media_url readback. Does not require an unrelated media gateway or new credentials.
- Original illustration PNG at `personas/mio/approved/assets/mio-evening-park-20260920.png`. [Image contract guard 35495515780](https://github.com/alston-personal/agentmanager/actions/runs/35495515780) passed four tests and verified public CDN bytes; [Core Social Runtime Guard 35495515775](https://github.com/alston-personal/agentmanager/actions/runs/35495515775) passed 29 tests. [Runtime rollout 35495583823](https://github.com/alston-personal/agentmanager/actions/runs/35495583823) returned success and verified both local and public endpoints.
- Manifest `personas/mio/approved/mio-post-20260920-evening-park-image-v1.json` pins `image_path` and `image_alt_text`; text lives in matching `.txt`. Approved post source commit `8a3272c06e154183cb3539149bde1fd8217e3e52` triggered [Actions run 35495831674](https://github.com/alston-personal/agentmanager/actions/runs/35495831674): `mio_approved_receipt=PASS`, `galaxy_day1_publish=PASS`, real ID/permalink above. Publisher fails IMAGE writes if public image digest cannot be fetched or Threads post.read does not confirm matching ID, image media_type IMAGE, visible HTTPS media URL and exact text. It NEVER downgrades to TEXT when image is required.
- Following that first real post, the workflow itself was strengthened (commit `9c5b8edc941c16f3dc15c75bb3a7ecff035833a0`) so future image jobs must surface explicit `galaxy_day1_image_asset=VERIFIED` and `galaxy_day1_image_readback=PASS` receipt markers, in addition to the post ID/permalink. These extra workflow assertions were added *after* the first successful real image run; do not retroactively claim they ran in 35495831674.
- Duplicate older image proposal [PR #405](https://github.com/alston-personal/agentmanager/pull/405) was closed as superseded to prevent accidentally merging its out-of-date branch.
- Persona event `threads-18356065492302036` with caption, image asset provenance, author, and verified URL was persisted in private `alston-personal/my-agent-data/main` commit `e8d9ca4aedb5c85b83ac9ac5e0dc6178f5aaaa77`.
- **Future operator path:** commit a new reviewed original PNG/JPEG under `personas/mio/approved/assets/` first; create a new `mio-post-<key>.json` manifest with `image_path` and `image_alt_text`; only *then* add exactly one matching new `mio-post-<key>.txt` under `personas/mio/approved/` on `core/integration` to trigger the publish workflow. Never reuse an existing post key; always verify the public Threads IMAGE receipt. MIME/digest/media type checks block accidental text-only fallback.


## Authentic photo stories and persistent wardrobe (policy v1)

Canonical Persona data, owned by `alston-personal/my-agent-data/main` (private):
- `personas/sunlake-milkcat/visual_content_policy.json`: real location photo + virtual Mio composite; weather/season guard, no fabricated live location/real-world purchase/usage, natural response to questions about source, proper disclosure of actual paid/gifted/affiliate relationship.
- `personas/sunlake-milkcat/wardrobe/item_schema.json`, `catalog.json`, `look_templates.json`, `README.md`: candidate→saved→approved_for_render→rendered_and_verified→retired product lifecycle; item/variant IDs, retailer, rights, dated pricing, commercial provenance and look/post usage. First three looks are styling drafts, **not shopped, worn or purchased items**. Catalog intentionally begins empty.
- `personas/sunlake-milkcat/visual_anchor_spec.json` currently says `reference_status: pending first approved semi-realistic anchor set`. Do not turn an unapproved randomly generated face into Mio's canonical likeness or publish a different face as identity-consistent. Request/locate user-approved identity references before photorealistic outfit posts.
- The above are persistent policy and data specifications, not a deployed wardrobe-shopping API or a confirmed automatic visual-identity match system. Any future `wardrobe.*` capabilities require separate implementation and acceptance.
- When using a real location photo background, record image rights, source, capture date if available, and place granularity. **Actual location photo does not mean Mio truly visited, paid for an item, or was photographed at that place**. Reply truthfully if asked. Set visual narrative to mood/composite rather than personal documentary.
- Do not assert brand relationships without verified contract and human approval; explicitly label sponsored, gifted or affiliate content when real. Never pretend Mio physically wore/tested a product.


## Image upload bridge + verified hiking-photo post (2026-09-20)

**READ THIS FIRST on any new chat before saying image transport is unavailable.** An actual chat-attached JPEG was transferred to the already working GitHub publisher WITHOUT Work or manual user upload. The connected GitHub API's `create_blob(encoding="base64")` accepts the exact bytes; the GitHub `create_file` wrapper only accepts UTF-8 text and MUST NOT be used for JPEG/PNG.

1. Obtain the **user-approved original attachment** (the chat upload, not an existing unrelated library illustration). If a container is available, copy its exact bytes to the requested filename, verify original and copy SHA256, base64-encode without loss. If the tools are split, temporarily transfer complete base64 as a UTF-8 file using the connected Files Library: `files.manage_library upload` -> `files.read(full)` -> connected `GitHub.create_blob({repository_full_name:"alston-personal/agentmanager",encoding:"base64",content:<complete_b64>})`. Read the **complete** base64 and strip only line whitespace; never log the base64 to the user. Immediately delete the temporary Library transfer after a confirmed blob SHA. If direct container-to-connector file transfer is available, use it instead. This is the transfer bridge that worked for the hiking post.
2. In `core/integration`, create the binary image tree entry under `personas/mio/approved/assets/<unique-approved-name>.jpg` with `create_tree(base_tree_sha=latest_tree, tree_elements=[{path,mode:"100644",type:"blob",sha:<binary_blob_sha>}])`. Upload the original image *before* the trigger. Create the matching `mio-post-<unique-key>.json` with `image_path` and `image_alt_text`; verify image exists and bytes/MIME match. A git commit containing just the image/manifest does NOT trigger publication.
3. **LAST**, put exactly one new `personas/mio/approved/mio-post-<unique-key>.txt` in the final commit and advance `core/integration` via fast-forward. The workflow `.github/workflows/oracle-publish-mio-approved.yml` is triggered by the new `.txt`; it requires image CDN SHA match and Threads IMAGE readback. Never reuse the post key or re-push the same `.txt` expecting a new publication. If anything fails, read the run receipt and Threads by exact ID before retrying to avoid duplicates.
4. Inspect the resulting **push** run through `GitHub.fetch(https://api.github.com/repos/alston-personal/agentmanager/actions/runs?head_sha=<publish_commit_sha>)`, then `fetch_workflow_run_jobs` / `fetch_workflow_job_logs`. Require a real `galaxy_day1_image_asset=VERIFIED`, `galaxy_day1_image_readback=PASS`, `galaxy_day1_publish=PASS` or verified `ALREADY_PRESENT`, real platform ID and public permalink. A failing Actions conclusion may be a *post-publication receipt assertion failure*: never repost blindly.

**Actual hiking-photo delivery:** Original `mio-hike-20260920.jpg` was uploaded as Git blob `d85479140c49350890757a64ad962f677b024990` (679821 bytes). Manifest `mio-post-20260920-hike-image-v1.json` and matching caption were committed in `e498ca0cddeff5272d4223e1f4140e21959df966`. [Actions run 35506022699](https://github.com/alston-personal/agentmanager/actions/runs/35506022699) reported `galaxy_day1_image_asset=VERIFIED`, `galaxy_day1_image_readback=PASS`, `galaxy_day1_publish=PASS`, real Threads ID `18086248307310806`, permalink https://www.threads.com/@mio.milkcat/post/DdgZ-gaFB8L. The run's overall conclusion was **failure after actual publication** because the final assertion incorrectly required the previous handle `@sunlake.milkcat`. Source commit `c4d39a1d6b8bc66989806f9756cc73f4567f407f` corrected this to the observed `@mio.milkcat`; no retry or duplicate post was made. Subsequent commit `720ae4bfd4b0187a254e4f31a726f5a395c715fc` requires image contract regressions in every publish preflight, and `0009480ae2470c3e633fa6e1a7fba70cb99e536e` enables the IMAGE CI guard on live branch pushes.

**Reply patrol:** source commit `94cc6a9682bbd7115c6769296801a03cfdbd3616` forces monitoring the newest five owned posts even when Threads' `has_replies` flag lags. [Oracle monitor run 35506285280](https://github.com/alston-personal/agentmanager/actions/runs/35506285280) returned `galaxy_experiment_monitor_receipt=PASS`, `reply_count=19`, zero new snapshot replies, and one **previously observed but still pending** model-scheduled reply to commenter `lyraenpro` (comment `17908989162499571`). The latest status was `mio_social_pending=1` and `mio_social_outbound=DISCOVERY_UNAVAILABLE:threads_api_unavailable_http_500`. A scheduled owned-post reply is not yet a posted reply; read an actual platform reply ID and parent/text readback before marking done. Outbound discovery for commenting on *other people's posts* is separately BLOCKED at keyword search and has not passed live acceptance. Do not equate a green monitor install with successful unsolicited/outbound public comments.


## Second live IMAGE acceptance and outbound troubleshooting (2026-09-20 evening)

- **Second independently verified image+text public post**: `mio-post-20260920-evening-secondlook-v1` uses the previously approved original evening-park illustration (reused to test the shipping path, not represented as a newly generated image), with NEW caption. The reviewed manifest was committed in `c6f80433c8f8a4a0642e76abd1208e6562b428f9`; new text in source commit `678efaa4f7ac94bcbad89fc79236a0941dac2595` triggered [Actions run 35506995301](https://github.com/alston-personal/agentmanager/actions/runs/35506995301), conclusion SUCCESS; its job log emitted `mio_approved_receipt=PASS`, `galaxy_day1_image_asset=VERIFIED`, `galaxy_day1_image_readback=PASS`, `galaxy_day1_publish=PASS`, platform object `18445538716127069`, public permalink https://www.threads.com/@mio.milkcat/post/DdgcYW1FAsh. This verifies repeatability of approved IMAGE delivery on the current live branch. It does not demonstrate a newly generated illustration or scheduled automatic original-post creation.
- External discovery is a **separate capability**. Existing keyword.search consistently returned provider HTTP 500 after the first minimal documented field-set change `7b5cc26c4960ed51d49ecbed914a750dbe5aa2a4` and [monitor 35507098701](https://github.com/alston-personal/agentmanager/actions/runs/35507098701). The second guarded change `b3fd9b48574466466637ef3ba00450919c76c41a` added a versioned-path fallback strictly for HTTP 500; after successful Oracle social runtime rollout, [monitor 35507230854](https://github.com/alston-personal/agentmanager/actions/runs/35507230854) still logged `mio_social_outbound=DISCOVERY_UNAVAILABLE:threads_api_unavailable_http_500`. **Not repaired or accepted.** Investigate Meta app `threads_keyword_search` capability/permission and effective user token grant, provider-side restrictions and error subtype before further automated outbound replies. Do not substitute scraping or mass outreach for an approved API path.
- Owned-post monitor remains working and separate from public search, with `galaxy_experiment_monitor_receipt=PASS`; a queued or scheduled reply (e.g. to lyraenpro) is not a published reply until platform ID and matching parent/text readback appear.


## Definitive outbound-discovery permission diagnosis (2026-09-20)

- A sanitized read-only Meta `/debug_token` probe was implemented in `scripts/diagnose_mio_threads_search_scope_user.py` and deployed by the regular Oracle monitor installer. [Run 35507384746](https://github.com/alston-personal/agentmanager/actions/runs/35507384746) returned `mio_search_scope_probe=not_granted` on the **currently bound real Threads user token** while owned-post monitoring succeeded. This is an *actual effective OAuth grant deficiency*, not an absent GitHub/Oracle execution channel. The source code requesting `threads_keyword_search` in persona OAuth does NOT establish that the existing user token received it.
- The public keyword-search API returned HTTP 500 for this token, including after the reduced-fields and bounded versioned-path probes. Do not infer a working external discovery capability from the presence of a code path. In `2d334ec5` the useless alternate-version retry was removed; `d83eff2f` limits repeated failed external discovery checks to one per configured discovery window instead of every 10-minute monitor cycle.
- **Owner consent required:** confirm that the Meta app is allowed to request Threads keyword search, then reauthorize the same Persona account through the existing Galaxy Social OAuth browser handoff, accepting `threads_keyword_search`. NEVER copy OAuth secrets into the repo or ask the assistant to fabricate/approve this grant. Rerun the sanitized probe: require `mio_search_scope_probe=granted`, then a real read-only `mio_social_outbound=SEARCH_READ_PASS`; only then enable/accept the first approved reply to a separately authored third-party public post, with actual object ID and correct parent/text readback. A different app permission state may still block the API, so do not declare success from the scope check alone.


## Same-chat food-photo delivery after user-supplied uploads (2026-09-20)

The user supplied TWO ORIGINAL food JPEG files in the same ChatGPT conversation that had just successfully published Mio's hiking photo. **Never assert this is another chat or that GitHub tools vanished without checking current tool availability.** The user explicitly authorized publishing the after-hike meal photos; the already working GitHub connector and pinned IMAGE workflow were reused.

- Original uploads: `IMG_9357.jpeg` (green risotto with three scallops) and `IMG_9359.jpeg` (cake with cream and pistachios), each 1536x864. Because the approved public publisher currently supports ONE IMAGE, a non-generative two-photo vertical contact sheet was made with both original photos intact, divided by a narrow white gutter, at `mio-after-hike-meal-20260920.jpg` (1560x1768, 552394 bytes; SHA256 `e1494a409953b29a0a61331e06c44f79ccf2f1b2be041c7854ca44e127f10612`). Do not call this native Threads multi-image carousel support.
- Binary image entered GitHub via `create_blob(encoding="base64")`, blob SHA `eda5ec0b19563e4c54fe5385f96c76406c4386a4`. Exact image and matching IMAGE manifest were staged in commit `9bddeccc3986d78c1b0dad97fa5c2d62b01998c1` before adding the trigger .txt. The approved caption trigger commit is `e7252e2458ca216b8fd50adf794eddafa381151b`.
- **Live accepted receipt:** [GitHub Actions run #35507932838](https://github.com/alston-personal/agentmanager/actions/runs/35507932838) finished SUCCESS and printed `mio_approved_receipt=PASS`, `galaxy_day1_image_asset=VERIFIED`, `galaxy_day1_image_readback=PASS`, `galaxy_day1_publish=PASS`, Threads object `18102186647629092`, actual permalink https://www.threads.com/@mio.milkcat/post/DdgerB_FL5P .
- The temporary base64 Library transfer file was deleted following confirmed GitHub blob creation. The image and approved post remain pinned in GitHub. DO NOT reuse old published illustration for tests unless the user expressly approves. Never fabricate claims that a chat tool suddenly became unavailable without checking; current-turn proof is actual GitHub action receipt.


## First verified native TWO-IMAGE CAROUSEL from chat uploads (2026-09-22)

- **Public Threads result**: https://www.threads.com/@mio.milkcat/post/DdlUOQ4FAzx ; `platform_object_id=17873481411585255`, `media_type=CAROUSEL_ALBUM`, `carousel_child_count=2`. These are two independent original photos, NOT a vertical photo composite. User-approved caption begins `今天上山，最先來打招呼的是松鼠。` and describes a mountain squirrel followed by a tabby cat.
- Feature: [PR #408](https://github.com/alston-personal/agentmanager/pull/408) merged into `core/integration` as `effe2a88a639c817b9e670dacd5c704d672e66cc`; the three focused PR checks passed, and [Oracle Social Runtime rollout 35704343936](https://github.com/alston-personal/agentmanager/actions/runs/35704343936) completed SUCCESS. `CAROUSEL` is a CREATE parameter; Meta returns `CAROUSEL_ALBUM` on published post reads, so the publisher recognizes both values.
- Original user attachments were `IMG_9338.jpeg` (squirrel) and `IMG_9288.jpeg` (cat). Complete original JPEG bytes were transferred from this SAME ChatGPT conversation via temporary base64 Library files -> connected GitHub `create_blob(encoding="base64")` without making composite/generated illustrations; confirmed Git blob SHA: squirrel `61fe5fe9561f27dc9c937239c517c083badd839a`, cat `25ebd9b738229218b52669855908d86292b0cf66`. Stage commit `d55ff8ad954608692f1a5ac1020fa0e11dadf2a0` added both files under `personas/mio/approved/assets/` and matching `images[]` manifest. Trigger commit `acb76ec62337d1d8645a3495aeb83284bf221c3b` added exactly one approved `mio-post-20260922-mountain-animal-carousel-v1.txt`; temporary Library transfer files were moved to Trash after Git blob confirmation.
- **Important failure-vs-platform distinction:** [publish run 35704642738](https://github.com/alston-personal/agentmanager/actions/runs/35704642738) attempt 1 returned sanitized provider HTTP 400/OAuthException 100, attempt 2 timed out at the publisher's 20-second *local HTTP client* even though the platform later published. DO NOT retry on a timeout without an authenticated read-only check. A read-only account query with `fields=id,text,media_type,children,permalink` in [monitor run 35704950082 attempt 2](https://github.com/alston-personal/agentmanager/actions/runs/35704950082) proved `mio_carousel_read_probe=PASS` and `mio_carousel_existing_target=FOUND:17873481411585255:CAROUSEL_ALBUM:2:https://www.threads.com/@mio.milkcat/post/DdlUOQ4FAzx`.
- After confirmed platform presence, the same publish workflow was rerun for **readback/idempotency only**. [Run 35704642738 attempt 3](https://github.com/alston-personal/agentmanager/actions/runs/35704642738) completed SUCCESS, `mio_approved_receipt=PASS`, `galaxy_day1_image_asset=VERIFIED`, `galaxy_day1_publish=ALREADY_PRESENT` with the SAME platform ID and permalink. No duplicate post was sent. This is proof of ONE actual original 2-image CAROUSEL publication, not proof of 20-image live acceptance or automatic recurring authoring.
- Preserve the **chat attachment -> temporary Library base64 -> Git blob -> stage both original assets+ordered images[] manifest -> exactly one new .txt trigger -> Oracle receipt -> authentic Threads post.read** workflow. Read full base64 in one `functions.exec` isolate and pass its string directly to `GitHub.create_blob`; never print/image-leak the base64 and never let a 20-second local request timeout masquerade as platform non-publication.

## 2026-09-25｜澪 Persona OAuth 一鍵授權入口

- Studio Web `main` 已新增 Galaxy 專用按鈕「為澪重新授權 Threads（搜尋與互動）」，位於 `https://studio.milkcat.org/threads-galaxy/` 的 Threads 連結區與已連結帳號的操作區。按鈕明確選擇 `auth_profile=persona`，而不是既有一般「連結／讀取我的 Threads」的 `viewer`。Studio Web UI 提交 `452c8c5080d436a9469087cb9e3281152ede7b44`。
- 既有 Oracle Static 工作流程 [Run #36075182707](https://github.com/alston-personal/agentmanager/actions/runs/36075182707) 完成 `threads_galaxy_static_receipt=PASS` 與 `threads_galaxy_public_acceptance=PASS`；這只驗證網站部署，不代表使用者已同意 OAuth 或 Meta 已核發 `threads_keyword_search`。
- **下一步只需由帳號持有人操作**：在 Safari 開啟上列 Galaxy 網址，點「為澪重新授權 Threads（搜尋與互動）」，於同一瀏覽器內按「繼續 Threads 網頁授權」，核對登入的是澪既有的 Threads 帳號，查看並同意 Meta 當次實際提供的授權範圍，完成返回 Galaxy。不要使用一般 viewer 連結按鈕假裝升級成功。勿分享權杖、OAuth code 或 App Secret。
- 授權回來後執行 Oracle 既有清理過的 `scripts/diagnose_mio_threads_search_scope_user.py`，要求 `mio_search_scope_probe=granted`，再執行實際 **唯讀** `keyword.search`，確認結果與有效帳號綁定。若 Meta 同意畫面未包含此 scope 或拒絕，先由 Meta App 管理者檢查應用程式權限／審查／角色或測試員邀請，不能靠重試其他 URL 繞過。
- 搜尋可用不等於取得任意第三人私密留言或自動到其他貼文留言的全面授權；後續外部回覆須獨立驗證允許的 Threads API 能力與平台回執。
