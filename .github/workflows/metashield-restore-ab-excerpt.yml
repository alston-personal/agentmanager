name: MetaShield Restore AB Excerpt

on:
  push:
    paths:
      - '.github/workflows/metashield-restore-ab-excerpt.yml'
      - 'scripts/metashield_restore_ab_excerpt.sh'

permissions:
  contents: write

jobs:
  probe:
    runs-on: [self-hosted, Linux, ARM64, oracle]
    timeout-minutes: 8
    env:
      DEPLOY_HOST: 127.0.0.1
      DEPLOY_USER: ${{ secrets.AGENTOS_DEPLOY_USER || secrets.N8N_DEPLOY_USER }}
      DEPLOY_SSH_PORT: ${{ secrets.AGENTOS_DEPLOY_SSH_PORT || secrets.N8N_DEPLOY_SSH_PORT }}
    steps:
      - uses: actions/checkout@v4
      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.AGENTOS_DEPLOY_SSH_KEY || secrets.N8N_DEPLOY_SSH_KEY }}
      - name: Extract restore AB source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .agentos/evidence
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new scripts/metashield_restore_ab_excerpt.sh "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield_restore_ab_excerpt.sh"
          ssh -p "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}" "bash /tmp/metashield_restore_ab_excerpt.sh /tmp/metashield-restore-ab-excerpt.txt"
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield-restore-ab-excerpt.txt" .agentos/evidence/metashield-restore-ab-excerpt.txt
      - name: Persist evidence
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'MetaShield Oracle'
          git config user.email 'register@milkcat.org'
          git add .agentos/evidence/metashield-restore-ab-excerpt.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'evidence(metashield): persist restore AB source excerpt'
          git push origin HEAD:ops/metashield-inspect-after-timeout
