name: MetaShield Recovery Function Contract

on:
  push:
    paths:
      - '.github/workflows/metashield-recovery-function-contract.yml'
      - 'scripts/metashield_recovery_function_contract.sh'

permissions:
  contents: write

jobs:
  contract:
    runs-on: [self-hosted, Linux, ARM64, oracle]
    timeout-minutes: 8
    env:
      DEPLOY_HOST: 127.0.0.1
      DEPLOY_USER: ${{ secrets.AGENTOS_DEPLOY_USER || secrets.N8N_DEPLOY_USER }}
      DEPLOY_SSH_PORT: ${{ secrets.AGENTOS_DEPLOY_SSH_PORT || secrets.N8N_DEPLOY_SSH_PORT }}
    steps:
      - uses: actions/checkout@v4
      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.AGENTOS_DEPLOY_SSH_KEY || secrets.N8N_DEPLOY_SSH_KEY }}
      - name: Probe recovery function contract
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .agentos/evidence
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new scripts/metashield_recovery_function_contract.sh "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield_recovery_function_contract.sh"
          ssh -p "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}" "bash /tmp/metashield_recovery_function_contract.sh /tmp/metashield-recovery-function-contract.json"
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield-recovery-function-contract.json" .agentos/evidence/metashield-recovery-function-contract.json
          cat .agentos/evidence/metashield-recovery-function-contract.json
      - name: Persist contract
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'MetaShield Oracle'
          git config user.email 'register@milkcat.org'
          git add .agentos/evidence/metashield-recovery-function-contract.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'evidence(metashield): persist recovery function contract'
          git push origin HEAD:ops/metashield-inspect-after-timeout
