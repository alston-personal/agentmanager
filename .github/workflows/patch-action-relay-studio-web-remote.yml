name: Patch Action Relay Studio Web Remote

on:
  push:
    paths:
      - '.github/workflows/patch-action-relay-studio-web-remote.yml'
      - 'scripts/patch_action_relay_studio_web_remote.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply deterministic narrow action patch
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/patch_action_relay_studio_web_remote.py
          python3 -m py_compile agentos_node/action_relay.py
          grep -q 'github.repo.ensure_studio_web' agentos_node/action_relay.py
      - name: Commit patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add agentos_node/action_relay.py
          git diff --cached --quiet && exit 0
          git commit -m 'feat(agentos): add governed Studio Web remote action'
          git pull --rebase origin main
          git push origin HEAD:main
