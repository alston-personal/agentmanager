name: Oracle Release Layout Lab v0.7 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical source notice
        run: |
          echo 'RETIRED: this historical workflow must not deploy LayoutLib assets from agentmanager/web_assets.'
          echo 'Use .github/workflows/oracle-release-layoutlab-v079-canonical.yml instead.'
          echo 'Canonical asset authority: alston-personal/layoutlib.'
