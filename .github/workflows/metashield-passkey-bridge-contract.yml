name: MetaShield Passkey Bridge Contract

on:
  push:
    paths:
      - '.github/workflows/metashield-passkey-bridge-contract.yml'
      - 'scripts/metashield_passkey_bridge_contract.sh'

permissions:
  contents: write

jobs:
  contract:
    runs-on: [self-hosted, Linux, ARM64, oracle]
    timeout-minutes: 8
    env:
      DEPLOY_HOST: 127.0.0.1
      DEPLOY_USER: ${{ secrets.AGENTOS_DEPLOY_USER || secrets.N8N_DEPLOY_USER }}
      DEPLOY_SSH_PORT: ${{ secrets.AGENTOS_DEPLOY_SSH_PORT || secrets.N8N_DEPLOY_SSH_PORT }}
    steps:
      - uses: actions/checkout@v4
      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.AGENTOS_DEPLOY_SSH_KEY || secrets.N8N_DEPLOY_SSH_KEY }}
      - name: Probe Passkey bridge contract
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .agentos/evidence
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new scripts/metashield_passkey_bridge_contract.sh "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield_passkey_bridge_contract.sh"
          ssh -p "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}" "bash /tmp/metashield_passkey_bridge_contract.sh /tmp/metashield-passkey-bridge-contract.json"
          scp -P "${DEPLOY_SSH_PORT:-22}" -o StrictHostKeyChecking=accept-new "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/metashield-passkey-bridge-contract.json" .agentos/evidence/metashield-passkey-bridge-contract.json
          cat .agentos/evidence/metashield-passkey-bridge-contract.json
      - name: Persist contract
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'MetaShield Oracle'
          git config user.email 'register@milkcat.org'
          git add .agentos/evidence/metashield-passkey-bridge-contract.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'evidence(metashield): persist Passkey bridge contract'
          git push origin HEAD:ops/metashield-inspect-after-timeout
